//models
const categoryModel = require('../models/category.model');


const debugLog = require('../middlewares/debugLog');

exports.saveCategory = (req, res) => {//api to create category
    console.log('save category functionc alled+++++++++++', req.body)
}
